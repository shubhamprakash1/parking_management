<div class="row">
<p id="notice"><%= notice %></p>
  <div class="container">
    <h1>Listing Events</h1>
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <div class="col-md-12 col-xs-12">
            <tr>
              <th>Name</th>
              <th>Description</th>
              <th>Schedule at</th>
              <th>Map</th>
              <th colspan="3"></th>
            </tr>
          </div>
        </thead>

        <tbody>
          <% @events.each do |event| %>
            <tr>
              <td><%= event.name %></td>
              <td><%= event.description %></td>
              <td><%= event.schedule_at %></td>
              <td><%= event.map %></td>
              <td><%= link_to 'Show', event, class: "btn btn-default btn-success" %></td>
              <td><%= link_to 'Edit', edit_event_path(event), class: "btn btn-default btn-primary" %></td>
              <td><%= link_to 'Destroy', event, class: "btn btn-default btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <br>
    <%= link_to 'New Event', new_event_path %>
  </div>
</div>
